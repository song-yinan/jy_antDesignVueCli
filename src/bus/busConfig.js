/**
  bus事件调用配置信息
    Demo:
     事件触发： this.$bus.emit("eventName");
     事件注册: this.$bus.on("eventName", () => {
          this.event();
      });
 */
export const eventName = {
  demo: "demo" // demo
};
